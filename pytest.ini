[pytest]
# 'auto' 模式会自动处理所有被 @pytest.mark.asyncio 标记的测试用例
asyncio_mode = auto

# 指定 pytest 应在哪个目录下查找测试文件
# 这是一个好习惯，可以避免意外地运行非测试目录下的文件
testpaths = tests

# 命令行附加选项
# --cov=src: 指定要计算覆盖率的目录
# --cov-report=term-missing: 在终端打印覆盖率报告，并高亮显示未覆盖的代码行
addopts = --cov=src --cov-report=term-missing
